services:
  workpay:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - workpay-data:/data
    environment:
      - WORKPAY_DATA_DIR=/data
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-password}
    restart: unless-stopped

volumes:
  workpay-data:
