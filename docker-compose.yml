services:
  workpay:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - workpay-data:/data
    env_file:
      - /etc/workpay.conf
    environment:
      - WORKPAY_DATA_DIR=/data
    restart: unless-stopped

volumes:
  workpay-data:
